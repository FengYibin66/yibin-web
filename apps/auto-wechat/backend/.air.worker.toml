root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/worker ./cmd/worker"
  bin = "./tmp/worker"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "testdata"]
  delay = 800

[misc]
  clean_on_exit = true
