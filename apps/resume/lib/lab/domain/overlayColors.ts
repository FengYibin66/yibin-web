/**
 * Lab 覆盖层与入口页的文案颜色，由 `__tests__/labContrast.test.ts` 按 WCAG 断言。
 *
 * 集中在这里是因为审计 E10 那两处低对比都是**内联样式里的一个数字**，
 * 改的时候凭手感调成「我这块屏上够了」就完了。改任何一个值之前先跑那条门禁。
 */

/** 走廊纸白——覆盖层文案的实际背景（取最亮的那面墙，最坏情况） */
export const CORRIDOR_PAPER = '#f4f0e8'

/**
 * 入口页的米色底。与 `CORRIDOR_PAPER` 只差一点，但**必须按各自的背景算**：
 * 用错背景得到的是一个看起来很精确的错数字。
 */
export const ENTRY_PAPER = '#f5f0e8'

export const OVERLAY_COLORS = {
  /** 走廊底部的操作提示。10px 小字，按正文要求 4.5 */
  hint: 'rgba(42,31,14,0.72)',

  /** 「← 退出 Lab」。深金：保住金色品牌口子的同时过 4.5 */
  exitLab: '#7a5d20',

  /**
   * 覆盖层里的次要说明文字（教程操作说明、兜底页正文、对话框提示），都是 9–12px。
   * 0.66 是**刚好过线**的一档（4.97）：再淡不达标，再深失去层次。
   */
  mutedText: 'rgba(42,31,14,0.66)',
} as const

/** 入口页的文案色（背景是 `ENTRY_PAPER`，与 Lab 分开算） */
export const ENTRY_COLORS = {
  /** 金色强调文字。`#846830` 是保住金色色相前提下最浅的达标值（4.62） */
  gold: '#846830',
  /** 方向标签的暖灰棕（4.64） */
  tag: '#7d6857',
} as const
