# apps/resume/scripts/qa/

给人看的验收工具。与 `__tests__/`、`e2e/` 的区别：**这里不断言**。

## 为什么要有这个目录

2026-09-07 的滚动显形事故：E2E 全绿、复现脚本全绿，但截图里项目卡半透明、hash 落在
半路、卡片被扭成 88°——任何人看一眼都能发现。原因不是缺一个用例，是缺一道工序：
改完视觉之后没有像用户一样把路走一遍、把每一屏看一遍。断言守的是**已知的坏法**，
巡检抓的是**没想到的坏法**；两者互补，缺一不可。

## 内容

```
walkthrough.mjs   用户路径巡检：门户 → Classic 滚到底 → 论文详情 → 返回 → 上滚 →
                  经历详情 → 返回 → 证书页 → 返回。每步一张整屏截图（默认 .qa/walk）
```

## 规则

- **改了 Classic / 门户的视觉或滚动行为，提 PR 前跑一遍并逐张看完。** 不是抽几张看，
  是全部。看的时候问三件事：内容在不在该在的位置、有没有半透明 / 扭曲 / 错位、
  有没有本该动画进入却直接杵着的东西。
- 发现问题先用 `e2e/` 或 `__tests__/` 把它变成断言，再修——巡检本身不守回归。
- 选择器一律 `:visible`：`TimelineItem` 同时渲染手机版与桌面版。
- 程序化滚动一律 `behavior: 'instant'`（Lenis 在场）；用户滚动用 `mouse.wheel`。
- 不要为了让脚本跑得快而减少截图步数：漏掉的那一屏就是没看的那一屏。

```bash
node scripts/qa/walkthrough.mjs                              # 打 dev
BASE=http://127.0.0.1:4321 node scripts/qa/walkthrough.mjs    # 打 E2E 的静态服务
```
